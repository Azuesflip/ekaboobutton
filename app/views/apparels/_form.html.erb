<%= form_with(model: apparel, local: true) do |form| %>
  <% if apparel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(apparel.errors.count, "error") %> prohibited this apparel from being saved:</h2>

      <ul>
      <% apparel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :apparel_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :apparel_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
